# =================================
# LMS Storage System Configuration
# =================================

# ---------------------
# Network Configuration
# ---------------------
# Thay đổi IP này khi deploy lên server khác
HOST_IP=192.168.1.49

# ---------------------
# Port Configuration
# ---------------------
BACKEND_PORT=8080
NGINX_PORT=8081
FRONTEND_PORT=5173

# ---------------------
# Backend Performance
# ---------------------
# Số worker xử lý ghi file đồng thời (30 = Gigabit speed)
FILE_WRITE_WORKERS=30

# Kích thước hàng đợi ghi file
WRITE_QUEUE_SIZE=500

# Kích thước buffer upload (64MB = 67108864 bytes)
UPLOAD_BUFFER_SIZE=67108864

# Kích thước buffer merge (64MB = 67108864 bytes)
MERGE_BUFFER_SIZE=67108864

# Số upload đồng thời tối đa
MAX_CONCURRENT_UPLOADS=50

# Timeout HTTP (giây)
HTTP_READ_TIMEOUT=600
HTTP_WRITE_TIMEOUT=600

# ---------------------
# Storage Configuration
# ---------------------
# Đường dẫn lưu trữ trong container (không nên thay đổi)
BASE_DIR=/app/file_uploads

# Docker volume name
VOLUME_NAME=lms-storage-data

# ---------------------
# Docker Network
# ---------------------
NETWORK_NAME=lms-network

# =================================
# NOTE:
# - Sau khi thay đổi HOST_IP, phải rebuild frontend: docker-compose up -d --build frontend
# - Thay đổi performance settings: docker-compose up -d --force-recreate storage-backend
# - Backup volume trước khi thay đổi quan trọng: xem DOCKER_COMPOSE_GUIDE.md
# =================================
