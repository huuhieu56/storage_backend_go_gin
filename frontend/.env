# =================================
# Frontend Environment Configuration
# =================================

# -------------------------------------
# 🖥️  LOCAL DEVELOPMENT (Host - npm run dev)
# -------------------------------------
# Uncomment these lines when running on host machine
# Vite dev server will proxy /api -> localhost:8080
# VITE_USE_PROXY=true

# -------------------------------------
# 🐳 DOCKER PRODUCTION (docker-compose)
# -------------------------------------
# Uncomment these lines when running in Docker
# Use relative paths, Nginx will handle routing
VITE_USE_PROXY=false

# NOTE: 
# - Host mode: npm run dev (Vite proxy to localhost:8080)
# - Docker mode: docker-compose up (Nginx proxy via Docker network)

